.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
/.idea/dictionaries
.DS_Store
/build

app/dualqrscanner-app.iml # Android Studio beta struggles with library projects outside the root directory; the workaround means this file changes every time, so we ignore it
